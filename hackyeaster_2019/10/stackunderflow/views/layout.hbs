<!DOCTYPE html>
<html lang="en">
    <head>
        <title>{{title}}</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
        <link rel='stylesheet' href='/stylesheets/style.css'/>
    </head>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="#">Stackunderflow</a>

            <ul class="navbar-nav mr-auto">
                <li class="nav-item {{#eq active 'home'}}active{{/eq}}">
                    <a href="/" class="nav-link">Home</a>
                </li>
                <li class="nav-item {{#eq active 'questions'}}active{{/eq}}">
                    <a href="/questions" class="nav-link">Questions</a>
                </li>
            </ul>

            {{#if username}}
                <span class="navbar-text">Logged in as {{username}}</span>
            {{/if}}

            <ul class="nav navbar-nav navbar-right">
                {{#if username}}
                    <li class="nav-item">
                        <a href="/logout" class="nav-link">Logout</a>
                    </li>
                {{else}}
                    <li class="nav-item {{#eq active 'login'}}active{{/eq}}">
                        <a href="/login" class="nav-link">Login</a>
                    </li>
                {{/if}}
            </ul>
        </nav>
    </header>
    <body>
        {{{body}}}
    </body>
</html>
